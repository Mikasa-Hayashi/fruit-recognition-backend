# Fruit Recognition Backend

## This is Backend directory written on FastAPI

### Endpoints:
1. GET `/api/v1/meta` 
2. GET `/api/v1/fruits`
3. POST `/api/v1/predictions`
4. GET `/api/v1/status`